# Dungeon Alea를 처음부터 다시 만든다면

이 문서는 현재 게임을 참고하되, 코드를 전부 지우고 처음부터 다시 만든다고 가정한 설계 가이드다.

대상 독자는 프로그래밍 비전공자다. 어려운 용어는 가능한 한 일상적인 말로 설명하고, 실제로 어떤 순서로 만들면 되는지 단계별로 정리한다.

---

## 1. 먼저 게임을 한 문장으로 정의한다

> 플레이어가 주사위를 굴려 점수 조합을 만들고, 지금 점수를 확정할지 더 굴려 전부 잃을 위험을 감수할지 선택하며, 라운드 사이에 아이템을 구매해 더 강한 조합을 만드는 게임.

이 한 문장은 개발 중 판단 기준이 된다.

- 새 기능이 이 재미를 강화하면 추가를 검토한다.
- 새 기능이 핵심 재미와 관계없으면 나중으로 미룬다.
- 그래픽이 없어도 이 선택이 재미있어야 한다.

이 게임의 핵심은 예쁜 주사위가 아니라 **“멈출까, 더 굴릴까?”라는 선택**이다.

---

## 2. 현재 프로젝트에서 배울 점

현재 프로젝트는 이미 다음 기능을 갖고 있다.

- Farkle 방식의 주사위 점수 계산
- 주사위 선택, 점수 확정, 다시 굴리기
- 라운드와 적 체력
- Joker, 숫자 강화, 특수 주사위
- 상점과 돈
- 게임 오버와 기록
- 애니메이션, 효과음, PWA, IndexedDB 저장

다만 처음부터 다시 만든다면 구조는 더 잘게 나누는 편이 좋다.

현재는 핵심 게임 규칙이 `src/game/logic.ts`에 비교적 잘 모여 있지만, 화면 표시와 애니메이션, 입력 처리, 상점 처리 등이 약 1,600줄의 `src/App.tsx` 안에 함께 있다. 기능이 늘어날수록 한 부분을 수정했을 때 다른 부분이 고장 날 가능성이 커진다.

또한 기획 문서와 실제 코드 사이에 차이가 있다. 예를 들어 Joker 슬롯 수, 보스 규칙, 시작 자금, 일부 Joker 효과가 문서와 코드에서 다르다. 다시 만들 때는 **코드보다 먼저 규칙표를 하나로 확정**해야 한다.

---

## 3. 비전공자가 알아야 할 최소 용어

| 용어 | 쉬운 설명 | 이 게임의 예 |
| --- | --- | --- |
| 상태(State) | 지금 게임이 어떤 상황인지 적어 둔 값 | 돈 5, 현재 점수 300, 남은 턴 2 |
| 규칙(Logic) | 어떤 행동을 했을 때 상태가 어떻게 바뀌는지 | Bank를 누르면 턴 점수가 라운드 점수에 더해짐 |
| 화면(UI) | 상태를 플레이어에게 보여주는 부분 | 주사위, 체력 바, 버튼 |
| 입력(Action) | 플레이어가 요청한 행동 | 주사위 선택, Roll, Attack |
| 이벤트(Event) | 게임 안에서 발생한 결과 알림 | Farkle 발생, Hot Dice 발생, 라운드 클리어 |
| 컴포넌트(Component) | 화면을 이루는 작은 부품 | 주사위 버튼, Joker 슬롯, 상점 카드 |
| 테스트(Test) | 규칙이 예상대로 작동하는지 자동 확인하는 코드 | `1,1,1`은 1,000점인지 확인 |

가장 중요한 구분은 다음과 같다.

> 규칙은 “무슨 일이 일어났는가”를 결정하고, 화면은 그것을 “어떻게 보여줄 것인가”를 결정한다.

예를 들어 Farkle 발생 여부는 규칙 코드가 결정해야 한다. 화면 코드는 그 결과를 받아 붉은 화면 효과와 소리를 재생하기만 해야 한다.

---

## 4. 만들기 전에 먼저 확정할 규칙

코드를 시작하기 전에 아래 항목을 표 하나로 확정한다. 중간에 규칙이 계속 바뀌면 코드 구조도 계속 흔들린다.

### 4.1 기본 진행 규칙

- 한 런은 몇 라운드까지 진행되는가?
- 한 라운드의 목표 점수는 어떻게 증가하는가?
- 라운드마다 턴은 몇 개인가?
- Bank와 Attack은 같은 행동인가?
- Farkle 시 현재 턴 점수만 잃는가?
- 모든 주사위를 점수로 사용하면 Hot Dice가 발동하는가?
- 첫 Roll은 반드시 점수 가능한 결과가 나오게 할 것인가?

### 4.2 점수 규칙

점수 계산 순서를 반드시 하나로 정한다.

권장 순서:

1. 선택한 주사위가 유효한 조합인지 확인한다.
2. 기본 점수를 계산한다.
3. 숫자 강화 효과를 적용한다.
4. 특수 주사위 효과를 적용한다.
5. Joker의 더하기 효과를 적용한다.
6. Joker의 곱하기 효과를 적용한다.
7. 최종 점수를 정수로 반올림한다.

이 순서를 문서와 코드에서 동일하게 유지해야 한다.

### 4.3 콘텐츠 규칙

- Joker 슬롯은 5개인가, 6개인가?
- 같은 Joker를 중복 구매할 수 있는가?
- Joker를 판매할 수 있는가?
- 특수 주사위는 영구적으로 교체되는가?
- 깨진 Glass 주사위는 다음 라운드에 복구되는가?
- 보스는 실제 규칙을 바꾸는가, 외형만 다른가?
- 상점에는 각 종류가 몇 개씩 등장하는가?

처음 버전에서는 콘텐츠 수를 작게 유지한다.

| 종류 | 첫 완성 버전 권장 수 |
| --- | ---: |
| Joker | 5개 |
| 특수 주사위 | 2개 |
| 숫자 강화 | 6개 |
| 보스 규칙 | 1개 |
| 라운드 | 3개 |

핵심 진행이 안정된 후 개수를 늘리는 것이 훨씬 쉽다.

---

## 5. 전체 구조: 식당처럼 역할을 나눈다

게임을 하나의 식당이라고 생각하면 이해하기 쉽다.

- **규칙 영역(Domain)**: 주문대로 음식을 만드는 주방
- **진행 영역(Application)**: 주문을 주방에 전달하고 결과를 정리하는 직원
- **화면 영역(UI)**: 손님이 보는 메뉴판과 테이블
- **저장 영역(Infrastructure)**: 재료와 기록을 보관하는 창고

권장 폴더 구조:

```text
src/
  app/
    App.tsx
    GameProvider.tsx
    gameReducer.ts

  domain/
    types.ts
    initialState.ts
    scoring.ts
    dice.ts
    round.ts
    shop.ts
    effects.ts
    commands.ts

  content/
    jokers.ts
    specialDice.ts
    bosses.ts
    balance.ts

  ui/
    screens/
      HomeScreen.tsx
      BattleScreen.tsx
      ShopScreen.tsx
      GameOverScreen.tsx
    components/
      DieButton.tsx
      ScorePanel.tsx
      EnemyPanel.tsx
      JokerSlot.tsx
      ShopCard.tsx
    hooks/
      useGameAnimation.ts
      useGameAudio.ts

  services/
    saveRepository.ts
    audioService.ts

  tests/
    scoring.test.ts
    commands.test.ts
    shop.test.ts
```

핵심 원칙은 다음과 같다.

- `domain`은 React, CSS, 소리, 타이머를 몰라야 한다.
- `ui`는 점수를 직접 계산하지 않아야 한다.
- `content`는 숫자와 설명을 모아 둔다.
- `services`는 저장, 소리처럼 브라우저 기능을 담당한다.

---

## 6. 게임 상태를 하나의 장부로 만든다

화면 곳곳에 값을 따로 보관하면 서로 다른 값이 표시될 수 있다. 게임 진행에 영향을 주는 값은 하나의 `GameState`에 모은다.

```ts
interface GameState {
  phase: GamePhase;
  run: RunState;
  turn: TurnState;
  dice: DieState[];
  inventory: InventoryState;
  shop: ShopState | null;
  meta: MetaState;
}
```

각 부분의 역할:

```ts
type GamePhase =
  | "home"
  | "rolling"
  | "choosing-dice"
  | "resolving-score"
  | "round-clear"
  | "shop"
  | "game-over";
```

```ts
interface RunState {
  round: number;
  targetScore: number;
  roundScore: number;
  totalScore: number;
  turnsLeft: number;
  money: number;
}
```

```ts
interface TurnState {
  score: number;
  rollCount: number;
  hadFarkle: boolean;
}
```

```ts
interface DieState {
  id: string;
  value: 1 | 2 | 3 | 4 | 5 | 6;
  type: SpecialDieId;
  status: "active" | "selected" | "locked" | "disabled";
}
```

여기서 `phase`가 특히 중요하다. 현재 게임이 어떤 단계인지 명확하면 다음과 같은 실수를 막을 수 있다.

- 굴리는 애니메이션 중 주사위를 다시 누르는 문제
- 상점이 열린 상태에서 Attack이 가능한 문제
- 게임 오버 뒤에도 Roll이 가능한 문제

---

## 7. 상태 변경은 Action으로만 요청한다

화면이 상태를 직접 이리저리 수정하지 않게 한다. 화면은 “무엇을 해 달라”고 요청하고, 규칙 코드가 결과를 만든다.

```ts
type GameAction =
  | { type: "START_RUN" }
  | { type: "ROLL_REQUESTED" }
  | { type: "DIE_TOGGLED"; dieId: string }
  | { type: "SCORE_CONFIRMED" }
  | { type: "BANK_REQUESTED" }
  | { type: "SHOP_ITEM_BOUGHT"; itemId: string; targetDieId?: string }
  | { type: "NEXT_ROUND_REQUESTED" };
```

그리고 하나의 함수가 요청을 처리한다.

```ts
function reduceGame(state: GameState, action: GameAction): GameResult {
  // 현재 상태와 행동을 받아 다음 상태와 이벤트를 반환한다.
}
```

결과는 상태만 반환하지 않고 이벤트도 함께 반환하는 편이 좋다.

```ts
interface GameResult {
  state: GameState;
  events: GameEvent[];
}

type GameEvent =
  | { type: "DICE_ROLLED"; dieIds: string[] }
  | { type: "SCORE_GAINED"; amount: number }
  | { type: "FARKLE_OCCURRED" }
  | { type: "HOT_DICE_OCCURRED" }
  | { type: "ROUND_CLEARED"; reward: number }
  | { type: "RUN_ENDED" };
```

화면은 이벤트를 보고 애니메이션과 소리를 실행한다.

- `FARKLE_OCCURRED` → 붉은 점멸 + Farkle 소리
- `SCORE_GAINED` → 점수 증가 애니메이션
- `ROUND_CLEARED` → 적 사망 애니메이션

이렇게 하면 규칙 코드 안에 `setTimeout`, 소리 재생, React 상태가 들어가지 않는다.

---

## 8. 점수 계산기는 가장 먼저, 가장 독립적으로 만든다

이 게임에서 가장 중요한 코드는 점수 계산기다. 화면보다 먼저 만들어야 한다.

점수 계산기는 같은 입력을 받으면 항상 같은 결과를 내야 한다.

```ts
interface ScoreInput {
  selectedDice: DieState[];
  upgrades: UpgradeState;
  jokers: JokerId[];
  context: ScoreContext;
}

interface ScoreResult {
  valid: boolean;
  baseScore: number;
  flatBonus: number;
  multiplier: number;
  finalScore: number;
  labels: string[];
  triggeredEffects: EffectId[];
}
```

예:

```ts
calculateScore({
  selectedDice: [die(1), die(1), die(1)],
  upgrades: noUpgrades,
  jokers: [],
  context: defaultContext
});
```

예상 결과:

```text
유효함: 예
기본 점수: 1000
최종 점수: 1000
설명: Three 1s
```

### 반드시 자동 테스트할 기본 사례

| 입력 | 예상 결과 |
| --- | ---: |
| `1` | 100 |
| `5` | 50 |
| `2` | 유효하지 않음 |
| `1, 5` | 150 |
| `2, 2, 2` | 200 |
| `1, 1, 1` | 1,000 |
| `1, 2, 3, 4, 5, 6` | 1,500 |
| `2, 2, 3, 3, 6, 6` | 1,500 |
| `2, 2, 2, 5` | 250 |
| `2, 2, 2, 3` 전체 선택 | 유효하지 않음 |

점수 계산 테스트가 없으면 Joker나 특수 주사위를 추가할 때 기존 점수가 조용히 망가질 수 있다.

---

## 9. Joker와 특수 주사위는 데이터와 효과로 만든다

Joker마다 별도의 복잡한 코드를 화면에 추가하면 콘텐츠가 늘어날수록 관리가 어려워진다.

Joker의 이름, 가격, 설명은 데이터로 둔다.

```ts
interface JokerDefinition {
  id: JokerId;
  name: string;
  price: number;
  description: string;
  effects: EffectDefinition[];
}
```

```ts
const greedy: JokerDefinition = {
  id: "greedy",
  name: "Greedy",
  price: 5,
  description: "굴린 횟수에 따라 점수 배율이 증가한다.",
  effects: [
    {
      timing: "score-multiplier",
      kind: "multiply-by-roll-count",
      amount: 1.2
    }
  ]
};
```

효과가 적용되는 시점을 정해 두면 충돌을 줄일 수 있다.

```text
before-roll
after-roll
before-score
flat-score
score-multiplier
after-score
on-farkle
on-bank
on-round-clear
```

처음부터 모든 효과를 완전히 범용 시스템으로 만들 필요는 없다. 하지만 최소한 “언제 발동하는 효과인지”는 통일해야 한다.

---

## 10. 무작위 결과도 통제할 수 있게 만든다

주사위 게임은 무작위 결과 때문에 버그 재현이 어렵다. `Math.random()`을 규칙 곳곳에서 직접 호출하지 말고, 주사위를 만드는 도구를 따로 전달한다.

```ts
interface RandomSource {
  rollDie(): 1 | 2 | 3 | 4 | 5 | 6;
  shuffle<T>(items: T[]): T[];
}
```

실제 게임에서는 진짜 무작위 도구를 사용한다. 테스트에서는 원하는 결과를 순서대로 내는 가짜 도구를 사용한다.

```text
테스트용 결과: 2, 3, 4, 6, 2, 3
예상: 점수 가능한 주사위가 없으므로 Farkle
```

이 방식이면 “가끔 발생하는 오류”를 정확히 다시 실행할 수 있다.

---

## 11. 화면은 작은 부품으로 나눈다

권장 화면 구성:

### HomeScreen

- 최고 점수
- 지난 점수
- 게임 시작 버튼

### BattleScreen

- 전체 점수와 라운드
- 적 정보와 체력
- 돈, 현재 턴 점수, 남은 턴
- Joker 슬롯
- 주사위 영역
- Roll / Attack 버튼

### ShopScreen

- 보유 돈
- Joker 상품
- 특수 주사위 상품
- 숫자 강화 상품
- 판매 확인창
- 다음 라운드 버튼

### GameOverScreen

- 최종 점수
- 최고 점수
- 다시 시작
- 홈으로 이동

각 화면 내부도 작게 나눈다.

```tsx
function BattleScreen() {
  return (
    <>
      <ScoreBanner />
      <EnemyPanel />
      <StatStrip />
      <JokerSlots />
      <DiceBoard />
      <BattleActions />
    </>
  );
}
```

`BattleScreen`은 점수 계산 방법을 알 필요가 없다. 이미 계산된 값과 가능한 행동만 전달받는다.

---

## 12. 애니메이션 상태는 게임 상태와 분리한다

다음 값은 실제 게임 규칙이 아니라 화면 연출이다.

- 주사위가 흔들리는 중인지
- Farkle 붉은 효과가 보이는지
- 적이 피격 애니메이션 중인지
- 상점이 등장하는 중인지
- 점수 숫자가 증가하는 중인지

이 값들은 저장할 필요가 없고, 게임 결과에도 영향을 주면 안 된다.

```ts
interface AnimationState {
  rollingDieIds: string[];
  showingFarkle: boolean;
  enemyAnimation: "idle" | "hit" | "dying" | "dead";
  floatingScores: FloatingScore[];
}
```

권장 흐름:

```text
플레이어가 Roll 클릭
→ 규칙 코드가 다음 상태와 DICE_ROLLED 이벤트 반환
→ 화면이 DICE_ROLLED 이벤트를 보고 300ms 애니메이션 재생
→ 애니메이션 종료 후 이미 결정된 주사위 값을 표시
```

애니메이션이 게임 결과를 결정하게 만들면 빠른 연속 클릭이나 타이머 순서 때문에 버그가 생기기 쉽다.

---

## 13. 저장은 “진행 데이터”만 저장한다

저장해야 하는 값:

- 현재 라운드와 점수
- 남은 턴과 돈
- 주사위 종류와 상태
- 보유 Joker
- 강화 상태
- 현재 상점 상품
- 최고 기록
- 저장 버전

저장하지 않아도 되는 값:

- 애니메이션 진행 시간
- 화면 점멸 여부
- 현재 재생 중인 소리
- 버튼을 누르는 효과

저장 데이터에는 반드시 버전을 넣는다.

```ts
interface SaveFile {
  version: 1;
  savedAt: number;
  game: GameState;
}
```

나중에 구조가 바뀌면 이전 저장 파일을 새 구조로 바꾸는 `migrateSave`가 필요하다.

저장 시점:

- Roll 결과가 확정된 뒤
- 점수를 Bank한 뒤
- Farkle 처리가 끝난 뒤
- 상품 구매 또는 판매 뒤
- 다음 라운드 시작 뒤
- 게임 오버 뒤

---

## 14. 구현 순서

처음부터 모든 기능을 동시에 만들지 않는다. 아래 단계마다 실제로 플레이 가능한 상태를 만든다.

### 1단계: 종이 위 규칙 확정

결과물:

- 한 페이지짜리 핵심 규칙표
- 점수 조합표
- 점수 계산 순서
- 첫 버전에 넣을 콘텐츠 목록

완료 기준:

- 같은 질문에 문서와 개발자가 서로 다른 답을 하지 않는다.

### 2단계: 화면 없는 점수 계산기

구현:

- 기본 주사위 조합 판정
- 선택 유효성 검사
- 기본 점수 계산
- 자동 테스트

완료 기준:

- 기본 점수 사례 테스트가 모두 통과한다.

### 3단계: 화면 없는 한 턴 진행

구현:

- Roll
- 주사위 선택
- 점수 확정
- 다시 Roll
- Bank
- Farkle
- Hot Dice

완료 기준:

- 명령을 순서대로 호출해 한 턴을 끝낼 수 있다.
- Farkle과 Hot Dice를 테스트로 강제로 발생시킬 수 있다.

### 4단계: 최소 전투 화면

구현:

- 숫자로 표시되는 주사위
- 선택 표시
- 현재 점수
- Roll / Bank 버튼

이 단계에서는 이미지, 소리, 복잡한 애니메이션을 넣지 않는다.

완료 기준:

- 모바일 화면에서 핵심 게임을 처음부터 끝까지 플레이할 수 있다.

### 5단계: 라운드와 게임 오버

구현:

- 목표 점수
- 남은 턴
- 라운드 클리어
- 다음 라운드
- 게임 오버

완료 기준:

- 3개 라운드를 정상 진행하고 실패할 수 있다.

### 6단계: 상점과 성장

구현 순서:

1. 돈과 상품 구매
2. 숫자 강화
3. 간단한 Joker 2개
4. 특수 주사위 1개
5. 판매

완료 기준:

- 구매 전후의 점수 차이가 테스트로 확인된다.

### 7단계: 콘텐츠 확장

한 번에 하나씩 추가한다.

```text
정의 추가
→ 효과 구현
→ 자동 테스트
→ UI 표시
→ 실제 플레이 확인
```

### 8단계: 연출과 소리

구현:

- 주사위 Roll 애니메이션
- 점수 증가 표시
- Farkle 효과
- 적 피격과 사망
- 상점 전환
- 효과음

완료 기준:

- 연출을 꺼도 게임 규칙이 동일하게 작동한다.

### 9단계: 저장과 PWA

구현:

- IndexedDB 저장
- 저장 버전과 이전 버전 변환
- 앱 설치용 manifest
- 오프라인 캐시

완료 기준:

- 새로고침 뒤 진행이 복구된다.
- 오프라인에서도 첫 화면과 저장된 게임이 열린다.

---

## 15. 테스트 계획

테스트는 “프로그램이 대신 눌러 보고 결과를 확인하는 것”이다.

### 규칙 테스트

- 모든 기본 점수 조합
- 잘못된 주사위 선택
- Hot Dice
- Farkle
- 마지막 턴 Bank
- 목표 점수 달성
- 턴 소진 후 게임 오버

### 콘텐츠 테스트

- 각 Joker가 정확한 시점에 한 번만 발동하는지
- 여러 배율의 적용 순서
- Glass, Zombie 등 특수 주사위의 수명
- 강화 구매 전후 점수

### 상점 테스트

- 돈이 부족하면 구매 불가
- 이미 구매한 상품은 재구매 불가
- 슬롯이 가득 차면 Joker 구매 불가
- 판매 금액이 정확함

### 저장 테스트

- 저장 후 불러오면 같은 진행 상태인지
- 이전 버전 저장 파일이 변환되는지
- 손상된 저장 파일에서 새 게임을 시작할 수 있는지

### 화면 테스트

- 애니메이션 중 중복 클릭 방지
- 상점 중 전투 버튼 비활성화
- 모바일 세로 화면에서 버튼이 잘리지 않음
- 키보드만으로 주요 버튼 사용 가능

---

## 16. 균형 조정은 코드 수정이 아니라 표 수정으로 한다

라운드 목표, 가격, 보상 같은 숫자는 한 파일에 모은다.

```ts
export const BALANCE = {
  startingMoney: 3,
  baseTurnLimit: 5,
  jokerSlotCount: 6,
  roundTargets: [1000, 1500, 2200],
  clearReward: 2,
  cleanClearReward: 2
};
```

콘텐츠 정의도 가능한 한 표처럼 읽혀야 한다.

그러면 다음 작업이 쉬워진다.

- 가격만 빠르게 조정
- 라운드 난이도 비교
- 테스트에서 같은 값을 사용
- 기획 문서와 실제 수치 비교

게임 플레이 기록도 남기면 좋다.

```text
라운드별 평균 점수
평균 Roll 횟수
Farkle 발생 횟수
가장 많이 산 Joker
게임 오버가 가장 많이 발생한 라운드
```

이 기록을 보고 난이도를 조정해야 한다. 느낌만으로 숫자를 바꾸면 원인을 찾기 어렵다.

---

## 17. 처음부터 피해야 할 실수

### 큰 `App.tsx` 하나에 모두 넣기

처음에는 빠르지만 기능이 늘수록 수정이 어려워진다. 화면, 규칙, 저장, 연출을 분리한다.

### 화면에서 점수를 다시 계산하기

규칙 코드와 화면 코드가 서로 다른 점수를 보여줄 수 있다. 점수 계산기는 한 곳에만 둔다.

### 기획 문서와 코드의 숫자를 따로 관리하기

슬롯 수나 가격이 서로 달라진다. 실제 숫자는 `content`와 `balance` 파일을 기준으로 삼는다.

### 테스트 전에 Joker를 많이 추가하기

효과가 서로 충돌했을 때 원인을 찾기 어렵다. 기본 규칙 테스트를 먼저 만든다.

### 애니메이션 타이머로 규칙을 진행하기

연속 클릭, 느린 기기, 화면 전환에서 문제가 생긴다. 결과는 규칙 코드가 먼저 결정하고 연출은 따라오게 한다.

### 무작위를 직접 여기저기서 호출하기

오류를 다시 만들기 어렵다. 무작위 도구를 한 곳으로 모은다.

### 첫 버전부터 완벽한 범용 효과 시스템 만들기

필요 이상으로 복잡해질 수 있다. 실제 Joker 5개를 안정적으로 표현할 정도만 만들고 확장한다.

---

## 18. 권장 첫 버전 범위

처음 다시 만들 때 목표는 현재 게임의 모든 기능 복제가 아니다. 먼저 작고 완성된 게임을 만든다.

```text
주사위 6개
기본 Farkle 점수 규칙
Roll / 선택 / Bank
Farkle / Hot Dice
3개 라운드
Joker 5개
숫자 강화 6개
특수 주사위 2개
간단한 상점
게임 오버
로컬 저장
모바일 세로 화면
```

보스 전용 규칙, 많은 연출, PWA 오프라인 기능, Joker 판매 등은 핵심 버전이 안정된 뒤 추가해도 된다.

---

## 19. 최종 설계 확인표

개발 중 아래 질문에 모두 “예”라고 답할 수 있어야 한다.

- [ ] 점수 계산기는 React 없이 실행할 수 있는가?
- [ ] 같은 입력을 주면 점수 계산 결과가 항상 같은가?
- [ ] 무작위 결과를 테스트에서 원하는 값으로 고정할 수 있는가?
- [ ] 화면은 점수를 직접 계산하지 않는가?
- [ ] 애니메이션을 제거해도 게임이 정상 진행되는가?
- [ ] Joker 효과가 발동하는 시점이 명확한가?
- [ ] 게임 진행 상태와 화면 연출 상태가 분리되어 있는가?
- [ ] 저장 데이터에 버전이 있는가?
- [ ] 기본 점수와 핵심 진행에 자동 테스트가 있는가?
- [ ] 가격과 목표 점수가 한 곳에 모여 있는가?
- [ ] 각 화면과 주요 UI 부품이 별도 컴포넌트인가?
- [ ] 문서의 규칙과 실제 게임의 규칙이 일치하는가?

---

## 20. 가장 중요한 결론

이 게임을 다시 만들 때 가장 먼저 해야 할 일은 그래픽 작업도, Joker 추가도 아니다.

1. 규칙을 하나의 문서로 확정한다.
2. 화면 없이 점수 계산기와 턴 진행을 만든다.
3. 규칙을 자동 테스트한다.
4. 최소 화면을 연결해 실제 재미를 확인한다.
5. 그 다음에 상점, 성장, 콘텐츠, 연출, 저장을 순서대로 추가한다.

좋은 구조의 기준은 파일이 많거나 어려운 기술을 쓰는 것이 아니다.

> 새 Joker 하나를 추가하거나 점수 규칙 하나를 바꿨을 때, 어디를 수정해야 하는지 명확하고 기존 기능이 고장 나지 않는 구조가 좋은 구조다.
